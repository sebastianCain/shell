#include "exec.h"
#include "redir.h"
#include "pipes.h"

void showPrompt() {
  char prompt[50];
  prompt = strcat(uname(), getcwd());
  printf("%s", strcat(prompt, "$ "));
}

int main() {
  showPrompt();
}
